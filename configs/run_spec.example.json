{
  "dataset_path": "src/bo_workflow/problems/her/data/HER_virtual_data.csv",
  "target_column": "Target",
  "objective": "max",
  "seed": 7,
  "num_initial_random_samples": 8,
  "default_batch_size": 2,
  "max_categories": 64,
  "max_features": 8,
  "intent": {
    "user_prompt": "Optimize HER yield from this dataset with a BO loop.",
    "notes": "Proxy oracle only for now."
  }
}
